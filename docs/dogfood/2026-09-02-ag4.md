# AG4 helper-default dogfood evidence

Date: 2026-09-02

Implementations:

- `6a6c166` (`transport: make supervised helper the default`)
- `34f1471` (`transport: fence provisional connect failures`)
- `98faa58` (`transport: harden reconnect cleanup and test gates`)

## Environment

- Host: Linux 7.0.10, x86_64
- Emacs: GNU Emacs 30.2
- Doom core: `b3a119823faced6c5768bb9d749dd57d300cacb5`
- Doom configuration: `98dcb437b57edb761eb96a950bc26a50a9cf13af`
- Nix: 2.34.7
- `ejn-helper`: 0.1.0, protocol v1
- Remote profile: test-local override of `mother` to `mother.lan`, using a
  structured Python argv and `/home/dcol/.ssh/config`

The user's Doom profile still contains the rejected legacy
`:jupyter-command` field.  The isolated test daemon replaced it with this
structured argv without changing or committing the user's dotfiles:

```text
env NIX_PATH=nixpkgs=flake:nixpkgs nix shell --impure --expr
with import <nixpkgs> {}; python3.withPackages (ps: with ps; [ jupyter ipykernel numpy matplotlib ])
-c python
```

## Remote gate

Command:

```sh
env \
  DOOMDIR=/home/dcol/dotfiles/.doom.d \
  DOOM_INIT_DIRECTORY=/home/dcol/.config/emacs \
  EJN_DOOM_E2E_PROFILE=mother \
  EJN_DOOM_E2E_HOST=mother.lan \
  EJN_DOOM_E2E_SSH_CONFIG=/home/dcol/.ssh/config \
  'EJN_DOOM_E2E_PYTHON_COMMAND_JSON=["env","NIX_PATH=nixpkgs=flake:nixpkgs","nix","shell","--impure","--expr","with import <nixpkgs> {}; python3.withPackages (ps: with ps; [ jupyter ipykernel numpy matplotlib ])","-c","python"]' \
  EJN_DOOM_E2E_TIMEOUT=120 \
  EJN_DOOM_E2E_CLEANUP_TIMEOUT=30 \
  EJN_DOOM_E2E_SHELL_TIMEOUT=240 \
  tests/run-doom-e2e.sh
```

Result: passed in an isolated Doom daemon.

The gate produced real PNG and text output and proved the published image kept
its original `image/png` descriptor.  It killed the source buffer without
shutting down the kernel, reopened the file, and reconnected to the same
kernel.  It then killed the exact local helper process and the exact local SSH
tunnel process in separate phases.  Production sentinels and automatic
reconnect rebuilt the local transport each time.  Each phase required the
exact submitted code to own the sole matching panel entry with terminal status
`ok`; a matching token elsewhere in panel history could not pass the gate.
The saved source text and unmodified flag were rechecked after initial output,
buffer reconnect, helper recovery, and tunnel recovery.

Identity and timing evidence:

| Phase | Helper PID | Tunnel PID | Recovery |
| --- | ---: | ---: | ---: |
| Cold connection | 297469 | 297466 | n/a |
| Buffer reconnect | 297489 | 297486 | 0.503 s |
| Helper killed and recovered | 297500 | 297497 | 1.078 s |
| Tunnel killed and recovered | 297511 | 297508 | 1.024 s |

The remote identity remained constant throughout:

- Remote PID: `297789`
- Session: `ejn-doom-e2e-ajDK3V-94e19db6a9f74affce4bc1a4f62353ca`
- Connection file:
  `/root/.cache/emacs-jupyter-notebook/kernel-ejn-doom-e2e-ajDK3V-94e19db6a9f74affce4bc1a4f62353ca.json`
- Helper state after every recovery: `ready`, protocol 1

The harness captured the durable direct-entry identity immediately after the
first helper attach and retained its source/registry evidence on any
unconfirmed cleanup.  Final cleanup first used the public explicit shutdown
only when the live entry still exactly matched that identity, then ran the
identity-bound SSH cleanup to synchronously remove the test-owned session
files.  A separate bounded SSH check returned exactly:

```text
PID_DEAD
ABSENT /root/.cache/emacs-jupyter-notebook/kernel-ejn-doom-e2e-ajDK3V-94e19db6a9f74affce4bc1a4f62353ca.json
ABSENT /root/.cache/emacs-jupyter-notebook/kernel-ejn-doom-e2e-ajDK3V-94e19db6a9f74affce4bc1a4f62353ca.log
ABSENT /root/.cache/emacs-jupyter-notebook/kernel-ejn-doom-e2e-ajDK3V-94e19db6a9f74affce4bc1a4f62353ca.pid
```

The execution deadline and cleanup deadline are separately bounded; cleanup
does not silently consume or extend the execution proof.

Final completion reruns used the packaged helper after the registry-response
framing and correlated-busy fixes.  Both isolated Doom daemons completed the
same cold-start, buffer-reattach, helper-loss, tunnel-loss, busy-tunnel-loss,
and explicit-shutdown sequence:

| Busy cell | Remote PID | Buffer reconnect | Helper recovery | Tunnel recovery | Busy recovery |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 3 s | 300004 | 0.802 s | 1.206 s | 1.224 s | 3.535 s |
| 125 s | 300133 | 0.803 s | 1.206 s | 1.327 s | 125.372 s |

Within each run the remote PID, randomized session id, and connection file
remained unchanged across every local transport replacement.  The 125-second
run therefore crosses the ordinary two-minute boundary while proving that a
transport outage during admitted kernel work neither replays nor loses that
work.  Both runs ended with the public explicit shutdown and an exact
test-owned remote cleanup check.

## Adversarial review

A Sol-max adversarial review found a pre-install race: helper or tunnel death
could arrive after `kernel_info` queued success but before the zero-delay
finalizer adopted the transport.  `34f1471` adds a synchronous transport
failure latch, fences already queued successes/events, correlates provisional
helper/tunnel ownership with the exact connect context, and revalidates both
resources at the installation boundary.  Deterministic tests cover helper
death, tunnel death, finalize-time revalidation, and the reentrant
success-then-failure ordering.

The same review drove the gate hardening recorded above: exact code/status
matching, repeated source immutability assertions, early cleanup identity
capture, exact session-file cleanup, and PID/cmdline ownership checks that
prevent the isolated Doom runner from terminating an unowned daemon.

`98faa58` then hardened the destructive replacement boundary.  Retry-fresh is
an exclusive transaction: it retains the exact registry entry until a bounded,
PID-and-argv-bound remote cleanup proves both process death and removal of the
JSON/log/PID paths.  Cancellation, timeout, malformed output, permission
denial, registry retirement failure, and stale callbacks start no replacement
and retain recovery authority.  Registry pruning now groups by the complete
generated SSH route and accepts a dead result only from a strict tri-state
response with one record per requested positive PID and one final terminator.
Missing, duplicate, contradictory, out-of-set, or otherwise malformed records
make the entire route unknown.

## Local gates

- Canonical source suite after deleting generated bytecode: 879/879 ERT tests,
  221 Python helper tests with three expected host-dependent skips, 25/25
  registry-worker tests, and 12/12 stress-runner tests.
- Real-Jupyter helper integration: 53/53 tests.  This includes the exact
  correlated `busy`, ordinary output, `execute_reply`, and terminal `idle`
  ordering used by the long live-recovery gate.
- Graphical panel gate: 100 image publications, 108 viewport materialization
  passes, zero post-warm image-cache variation, and zero retained entries or
  artifacts after cleanup.
- AG3 stress: five fresh-process repeats, 65/65 ERT executions.  Batches took
  22.946-23.057 seconds and covered hostile helper exits, a credited flood,
  64 MiB artifact spooling, 100-image bounded retention, relay outage with
  kernel identity preservation, helper death, and FIFO transport boundaries.
  An earlier five-repeat attempt passed batch 1 but Emacs exited with status
  `-13` in batch 2 because it inherited the supervisor's outer stdout/stderr.
  The supervisor now owns and nonblockingly drains both Emacs and relay output,
  enforces hard byte caps and an exact 13/13 summary, bounds all JSON and
  fixture capture, and bounds successful temp-tree deletion.  The final run
  passed all five batches without SIGPIPE.  Failed batches retain their private
  evidence directory after exact process-group cleanup; successful batches
  leave none.
- Production byte compilation passed with only the two known optional Evil
  variable warnings.  The legacy transport has been removed; production is
  helper-only.  All generated `.elc` files were removed before the final
  source run.
- `nix flake check --no-build` passed.  The x86_64-linux default runtime,
  helper, and registry-worker outputs built successfully, and the corresponding
  aarch64-darwin derivations evaluated successfully without cross-building.
- `git diff --check` passed.  Final local process and generated-artifact audits
  found no EJN helper, relay, test daemon, `.elc`, `.pyc`, or Nix `result`
  residue.

## Scope decisions

AG4 is complete.  The automated graphical gate covers 100-image viewport and
native-cache behavior, and the live 125-second gate covers a multi-minute busy
cell across transport loss.  The sole user explicitly waived physical
sleep/Wi-Fi testing, a week-long observation period, and migration of legacy
profiles or sessions as completion blockers.  Those waived observations are
not represented as test evidence.
