# AG4 helper-default dogfood evidence

Date: 2026-09-02

Implementation: `6a6c166` (`transport: make supervised helper the default`)

## Environment

- Host: Linux 7.0.10, x86_64
- Emacs: GNU Emacs 30.2
- Doom core: `b3a119823faced6c5768bb9d749dd57d300cacb5`
- Doom configuration: `98dcb437b57edb761eb96a950bc26a50a9cf13af`
- Nix: 2.34.7
- `ejn-helper`: 0.1.0, protocol v1
- Remote profile: test-local override of `mother` to `mother.lan`, using a
  structured Python argv and `/home/dcol/.ssh/config`

The user's Doom profile still contains the rejected legacy
`:jupyter-command` field.  The isolated test daemon replaced it with this
structured argv without changing or committing the user's dotfiles:

```text
env NIX_PATH=nixpkgs=flake:nixpkgs nix shell --impure --expr
with import <nixpkgs> {}; python3.withPackages (ps: with ps; [ jupyter ipykernel numpy matplotlib ])
-c python
```

## Remote gate

Command:

```sh
env \
  DOOMDIR=/home/dcol/dotfiles/.doom.d \
  DOOM_INIT_DIRECTORY=/home/dcol/.config/emacs \
  EJN_DOOM_E2E_SERVER=ejn-ag4-final-20260902 \
  EJN_DOOM_E2E_PROFILE=mother \
  EJN_DOOM_E2E_HOST=mother.lan \
  EJN_DOOM_E2E_SSH_CONFIG=/home/dcol/.ssh/config \
  'EJN_DOOM_E2E_PYTHON_COMMAND_JSON=["env","NIX_PATH=nixpkgs=flake:nixpkgs","nix","shell","--impure","--expr","with import <nixpkgs> {}; python3.withPackages (ps: with ps; [ jupyter ipykernel numpy matplotlib ])","-c","python"]' \
  EJN_DOOM_E2E_TIMEOUT=90 \
  EJN_DOOM_E2E_CLEANUP_TIMEOUT=20 \
  EJN_DOOM_E2E_SHELL_TIMEOUT=180 \
  tests/run-doom-e2e.sh
```

Result: passed in an isolated Doom daemon.

The gate produced real PNG and text output, killed the source buffer without
shutting down the kernel, reopened the file, and reconnected to the same
kernel.  It then killed the exact local helper process and the exact local SSH
tunnel process in separate phases.  Production sentinels and automatic
reconnect rebuilt the local transport each time.  A randomized kernel-side
token was executed successfully after buffer reconnect and after both fault
recoveries; the source buffer was not modified by those checks.

Identity and timing evidence:

| Phase | Helper PID | Tunnel PID | Recovery |
| --- | ---: | ---: | ---: |
| Cold connection | 260314 | 260311 | n/a |
| Buffer reconnect | 260334 | 260331 | 0.512 s |
| Helper killed and recovered | 260345 | 260342 | 1.158 s |
| Tunnel killed and recovered | 260356 | 260353 | 1.027 s |

The remote identity remained constant throughout:

- Remote PID: `296798`
- Session: `ejn-doom-e2e-h4zvZW-13a4811a8d3d11e6e843da86c9703f73`
- Connection file:
  `/root/.cache/emacs-jupyter-notebook/kernel-ejn-doom-e2e-h4zvZW-13a4811a8d3d11e6e843da86c9703f73.json`
- Helper state after every recovery: `ready`, protocol 1

The final cleanup was an explicit test-owned shutdown.  A separate bounded SSH
check returned `PID_DEAD` for PID 296798 and found no file containing the exact
session id beneath `/root/.cache/emacs-jupyter-notebook`.

## Local gates

- Canonical source suite after deleting generated bytecode: 818/818 ERT tests
  and 218/218 Python helper tests, with three expected host-dependent skips.
- AG3 stress: five fresh-process repeats, 65/65 ERT executions.  Batches took
  22.609-22.760 seconds and covered hostile helper exits, a credited flood,
  64 MiB artifact spooling, 100-image bounded retention, relay outage with
  kernel identity preservation, helper death, and FIFO transport boundaries.
  Two earlier orchestration attempts ended one batch with status `-13` while
  Emacs inherited the outer tool's output stream.  A focused signal trace did
  not observe SIGPIPE delivery, AG3 already binds the helper explicitly, and
  the subsequent isolated single repeat and required five-repeat run passed.
  This remains a stress-runner output-isolation risk, not a reproduced product
  or helper-default failure.
- Production byte compilation passed with only the two known optional Evil
  variable warnings.  The optional legacy remote suite retained four known
  adapter declaration warnings.  All generated `.elc` files were removed
  before the final source run.
- `git diff --check` passed.  Final local process and generated-artifact audits
  found no EJN helper, relay, test daemon, `.elc`, `.pyc`, or Nix `result`
  residue.

## Manual gaps

AG4 is not complete yet.  The following require real graphical use over time:

- Hours-long Wi-Fi loss and restoration while the registered kernel remains
  alive, including the idle-watchdog interaction.
- Real laptop sleep/resume.
- A multi-minute busy cell across a transport outage.
- Repeated reconnect cycles beyond the isolated helper/tunnel fault pair.
- Rapid scrolling over 100+ images in a graphical Doom frame, including native
  image-cache/RSS observation.  AG3 covers bounded retained state headlessly,
  not graphical cache behavior.
- Migration of the user's real Doom profile from `:jupyter-command` to the
  structured `:python-command` argv used above.
- At least one week of normal helper-default use before AG5 may remove the
  legacy emacs-jupyter transport.
