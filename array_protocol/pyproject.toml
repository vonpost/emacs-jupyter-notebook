[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ejn-array-protocol"
version = "0.1.0"
description = "Bounded numerical slice format for EJN"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["ejn_array"]
